// Vite client types: import.meta.env / import.meta.glob / asset module declarations
// (.svg imports etc.). This reference used to live in the auto-generated
// src/sst-env.d.ts, which was removed in the pre-public cleanup — keep it in the
// canonical Vite location instead. App-specific env vars are typed in env.d.ts.
/// <reference types="vite/client" />
